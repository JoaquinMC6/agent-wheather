[project]
name = "agent-wheather"
version = "0.1.0"
description = "Agent that answers questions about the temperature in different cities."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
]

[tool.uv.workspace]
members = [
    "agents/weather",
]
